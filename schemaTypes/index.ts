import {artist} from './artist'
import {label} from './label'
import {imageWithAlt} from './objects/imageWithAlt'
import {release} from './release'

export const schemaTypes = [imageWithAlt, artist, label, release]
